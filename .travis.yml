# vim:sw=2:et:

sudo: false

language: erlang
otp_release:
  - "17.5"
  - "18.3"
  - "19.1"

script:
  - make escript FULL=1
  - ./hexpm search rabbit_common
  - make tests FULL=1

addons:
  apt:
    packages:
      - p7zip-full
cache:
  apt: true

notifications:
  email:
    - alerts@rabbitmq.com
